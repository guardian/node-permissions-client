{
  "name": "node-permissions-client",
  "description": "Node library for the Guardian's permissions service",
  "version": "1.0.1",
  "license": "Apache-2.0",
  "main": "src/index.js",
  "engines": {
    "node": ">= 4.3"
  },
  "scripts": {
    "lint": "node ./node_modules/eslint/bin/eslint.js src test",
    "tap": "node ./node_modules/tap/bin/run.js test",
    "test": "npm run lint && npm run tap"
  },
  "devDependencies": {
    "aws-sdk": "^2.3.19",
    "eslint": "^2.12.0",
    "tap": "^5.7.2"
  },
  "peerDependencies": {
    "aws-sdk": "2.x"
  },
  "files": [
    "src"
  ]
}
